[package]
name = "ffi"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[lib]
crate-type = ["staticlib"]

[dependencies]
embedder = { path = "../embedder" }
ipc_messages = { path = "../ipc_messages" }

[build-dependencies]
cc = "1.2"