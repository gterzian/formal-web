[compile.any.js]
  expected: FAIL
  # 8 of 10 sub-tests now pass (Missing argument, Invalid arguments fixed by
  # returning rejected promises instead of throwing).
  # Empty buffer and Invalid code: CompileError instanceof check fails because
  # the Boa Error constructor does not support setting custom prototypes via
  # `new.target` in a way that satisfies the WPT harness's `instanceof` test.
  # TODO: fix CompileError prototype chain so instanceof WebAssembly.CompileError works.
  [Empty buffer]
    expected: FAIL
  [Invalid code]
    expected: FAIL
