[Worker-multi-port.html]
  disabled: worker.postMessage() with no arguments does not throw a TypeError (the required message argument is not enforced), and the multi-port subtests rely on the exception.
