[sending-messages.html]
  disabled: Flaky: races with worker close(). The worker posts a message after close() in the same task; when teardown wins the race the post-close message never arrives and the test times out (it passes in isolation). The worker close path must let the current script task finish and deliver the messages it posts; TODO: fix the close/teardown timing, then re-enable.
