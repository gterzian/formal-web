[005.html]
  disabled: Flaky: races with worker close(). The worker calls self.close() and then postMessage() in the same task; when teardown wins the race the message never arrives and the test times out (it passes in isolation). The worker close path must let the current script task finish and deliver the messages it posts; TODO: fix the close/teardown timing, then re-enable.
