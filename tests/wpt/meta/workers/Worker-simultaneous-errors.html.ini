[Worker-simultaneous-errors.html]
  disabled: Runtime errors thrown by concurrent workers are not reported: the test expects 4 error events at the Worker objects but 0 arrive (task exceptions are not reported to the Worker object).
