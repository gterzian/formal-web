[Worker_ErrorEvent_message.htm]
  disabled: Runtime errors thrown inside worker tasks are not reported: no error event fires at the worker global scope and none reaches the Worker object (the window's worker.onerror never fires); ErrorEvent is missing (see content/src/html/README.md, Workers).
