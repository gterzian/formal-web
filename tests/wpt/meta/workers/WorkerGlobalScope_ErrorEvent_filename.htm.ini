[WorkerGlobalScope_ErrorEvent_filename.htm]
  disabled: The worker's legacy onerror handler is never invoked for task exceptions, so the worker never posts back the error arguments (see content/src/html/README.md, Workers: runtime errors are conflated and ErrorEvent is missing).
